apiVersion: v1
kind: Service
metadata:
  name: swim-tfdm-consumer
  namespace: swim
spec:
  type: NodePort
  selector:
    app: swim-tfdm-consumer
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30080
